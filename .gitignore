.aider*
/target
.DS_Store
node_modules
_docs
.nx
.yarn
.pnp.*
*.log
dist/
*.tsbuildinfo

# runtime DB — never commit (was leaked to public history once, purged)
/livestack.sqlite

# Python bytecode
__pycache__/
*.pyc

# Python virtualenv — machine-local, never commit (accidentally swept in by a wip-sync auto-commit)
node-py/.venv/
