# Build receipt: scheduler-policy production artifacts (task 6.1), 2026-09-24

Artifacts are on xc-tower-ubuntu at `~/livestack-build-artifacts/scheduler-policy-9f210edf/` (not yet installed; installation is task 6.2). MANIFEST.txt, verbatim:

```
scheduler-policy production artifacts (openspec change scheduler-policy-routine, task 6.1)
source:       livestack main 9f210edf (git archive of HEAD; jingway-policy/-py pinned rev 3ae721fc8dcad671d91c0138fbc3b770696826b6)
build host:   xc-win-1 WSL2 Ubuntu (ssh -p 2222 ubuntu@100.64.0.1), x86_64, 16 cores, glibc 2.43
toolchain:    rustc 1.97.1 (8bab26f4f 2026-07-14), cargo 1.97.1, maturin 1.15.0
built:        2026-09-24
commands:     cd native/policy && CARGO_NET_GIT_FETCH_WITH_CLI=true maturin build --release -i python3.12
                (run from native/policy so pyproject.toml's manifest-path=py/Cargo.toml and
                 features pyo3/extension-module + abi3-py38 apply; ldd shows NO libpython dependency)
              cargo build --release -p livestack-policy-cli

files (sha256):
c49dc1fcd0473a1b4b29288e7d642196dd67dfbbc4b9c4f770e05816c2976924  livestack_policy.abi3.so
37d38dd98b428b375f1870f38b59d67d2a1605b85908d7b1cc4bf615be5984c9  livestack-policy
8c994a2654f5bb0039f57e9a85d7f79377d9bd0610b61bc5d6221cc393e6133a  livestack_policy-0.1.0-cp38-abi3-manylinux_2_34_x86_64.whl

glibc (max GLIBC_ symbol version, objdump -T):
  livestack_policy.abi3.so  GLIBC_2.34
  livestack-policy          GLIBC_2.34
  tower (xc-tower-ubuntu) ldd --version: 2.43  -> both OK (2.34 <= 2.43)
  dynamic deps (both): libgcc_s.so.1, libc.so.6, ld-linux-x86-64.so.2

python import layout:
  The wheel ships livestack_policy/{__init__.py, livestack_policy.abi3.so}. The .so ALONE works:
  copy livestack_policy.abi3.so bare into node-py/ (next to livestack_node/) and put node-py/
  on PYTHONPATH. No package dir, no __init__.py needed. Production code imports
  "from livestack_policy import policy" (policy_runtime.import_native).
  NOTE: families() lives on the "policy" submodule: livestack_policy.policy.families();
  the literal tasks.md smoke "livestack_policy.families()" raises AttributeError.

smoke (CPython 3.12.14 venv on build host; tower fleetd interpreter is 3.12.2):
  PYTHONPATH=node-py python -c "import livestack_policy; print(livestack_policy.policy.families())"
    -> [{'id': 'livestack.fleet.choose_target', 'param_space': {...13 params...}, 'version': 1}]
  release layout (node-py source @9f210edf + bare .so):
    import_native() -> native: True ['livestack.fleet.choose_target'] artifact_version fn: True Recorder: True
  ./livestack-policy families
    -> [{"id":"livestack.fleet.choose_target","param_space":{"params":[{"hard_max":10.0,...}]},"version":1}]
```
