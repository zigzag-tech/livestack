# Deploy receipt — tasks 6.2 and 6.4 (2026-09-24, operator-authorized)

## 6.2 compare-mode deploy on the fleet broker (xc-tower-ubuntu, livestack-fleetd)
- Release: `~/.local/share/livestack-releases/scheduler-policy-78fe4fb7/` (node-py + fleetd at
  `78fe4fb7`; native `livestack_policy.abi3.so` sha256 `c49dc1fc…6924` and CLI
  `livestack-policy` sha256 `37d38dd9…84c9` from build `9f210edf` — `native/` unchanged since).
- Drop-in: `/etc/systemd/system/livestack-fleetd.service.d/90-scheduler-policy.conf`
  (PYTHONPATH → the release; `LIVESTACK_POLICY_NATIVE=compare`; `LIVESTACK_POLICY_DIR`;
  records 128 MiB × 16; `LIVESTACK_POLICY_SELF_PRINCIPALS=policy-improver`).
- Auth: principal `policy-improver` (owner `livestack:policy`, capability `policy_admin`)
  added to `30-auth.conf` (backup `30-auth.conf.bak-pre-scheduler-policy-*`, mode 600 kept);
  token at `~/.config/livestack/policy-admin.token` (600).
- Live release previously `fleet-provisioning-a75ef34d-cold` carried two hand patches
  (cold-node admission in `fleet_admit.py`, `:path` lease routes in `hostd.py`); both are on
  main and in this release — checked before switching.
- **Rollback:** `sudo rm …/90-scheduler-policy.conf && sudo systemctl daemon-reload && sudo systemctl restart livestack-fleetd`.

Smoke (after restart):
- journal: `[policy] livestack.fleet.choose_target: source=file version=b3:b6d406f3…fd025 mode=compare native=True`;
  `[fleet-auth] token … (policy-improver) carries policy_admin`.
- First artifact published (v0 = compiled defaults, exploration off, created_by
  `human:settinghead`): version `b3:b6d406f37483a1715f45e28640bf386d841e1cf8c76b566e77992bd8900fd025`,
  identical to the compiled defaults' version (so routing is unchanged). `degraded: []`.
- `/fleet/admit` from `policy-improver` → `decision_id` returned; policy stream got the
  decision + `lease_held_s`, `lease_expired=0`, `caller_ok=1`, `job_wall_s` on release.
- **Bug found and fixed by this smoke:** outcomes were stamped with the broker's lease clock
  (`time.monotonic()`, ts ≈ 1.56e6). Fixed in `78fe4fb7` (regression test fails on the old
  code); redeployed; outcomes now stamped with wall time (checked: 2.0 s ago).
- `livestack-policy selfcheck` over the live stream: `{"checked":67,"failed":0,"self_check":"passed"}`.

## 6.4 improver timer
- User units `~/.config/systemd/user/livestack-policy-improver.{service,timer}`; daily 04:17,
  `Persistent=true`, linger on. Runs `npm run policy-improver -- --once` from the release's
  fleetd (jingway linked from `~/jingway`).
- First run: `NO_PROPOSAL` — ledger bootstrapped from the broker's artifact (no drift),
  69 decisions / 130 outcomes / 0 gaps, self-check passed; preconditions not met:
  0 joined non-self-traffic decisions (needs 2000) and exploration on for 0 (needs 0.8).
  Service `Result=success`.

## Still gated
- 6.3 (switch to `LIVESTACK_POLICY_NATIVE=auto`) needs ≥ 7 days of compare mode with 0 mismatches.
- 7.1/7.2/7.3 operator decisions. Note for 7.1: attune — the dominant caller — releases
  leases WITHOUT `{status, wall_s}`, so the proposed `job_wall_s` objective joins only for
  callers using `workloads/lease_helper.py`.

## 7.1 / 7.2 (2026-09-24)

Operator decisions (2026-09-24): **7.1** objective `min job_wall_s`, guardrails
`lease_expired` ≤ incumbent+0.01 and `caller_ok` ≥ incumbent−0.01 — CONFIRMED by
`human:settinghead`. **7.2** exploration ON: `epsilon=0.05`, `margin=0.25`.

- New path (a raw PUT would have made the broker disagree with the ledger →
  `policy_projection_drift`): `npm run policy-publish -- <artifact.json> [--confirm-objective]`
  (`fleetd/src/policy/publish.ts`, merge `e5981d37`). Same order as `policy-approve --activate`:
  validate via the binary, parent == ledger active, no drift, `human_patch_approval` first,
  then jingway's activation service (PUT first, record `active` second; a failed PUT is `blocked`).
- Release `~/.local/share/livestack-releases/scheduler-policy-e5981d37/` (native `.so` + CLI copied
  from `scheduler-policy-78fe4fb7`, sha256 `c49dc1fc…`/`37d38dd9…`; `git diff --stat 9f210edf e5981d37 -- native`
  and `78fe4fb7 e5981d37 -- node-py` both empty, so the broker drop-in and broker were NOT touched).
  Only the improver user unit was repointed (backup `livestack-policy-improver.service.bak-pre-e5981d37`).
- Artifact v1 = v0 + `exploration {enabled: true, epsilon: 0.05, margin: 0.25}`, parent v0,
  provenance `human:settinghead`: **`b3:a66da2950d88c2b4b0924d1cdf38f4b7f2c0f9ab7501cbefea1709343ab9dfd4`**
  (`livestack-policy artifact hash`; file `~/.local/share/livestack/policy-improver/v1/v1.json`).
  `policy-publish --confirm-objective` → exit 0.
- Ledger (improver DB): `1 activated v0 (bootstrap)`, `2 human_patch_approval v1 settinghead`,
  `3 superseded v0`, `4 activated v1 by human:settinghead, source code`. Active objective:
  `{"outcomeId":"job_wall_s","direction":"min","status":"confirmed","approvedBy":"human:settinghead"}`;
  guardrails `lease_expired`/`caller_ok` bound 0.01, `status: confirmed`. **7.1 Verify met.**
- Broker `GET /fleet/policy/livestack.fleet.choose_target`: source `file`, active v1
  (`version_verified: true`), previous v0, mode `compare`, native true, mismatches 0,
  `degraded: []`, **`exploration: "disabled:mode_compare"`**.
- Improver run (`systemctl --user start livestack-policy-improver.service`, Result=success):
  `NO_PROPOSAL`; ledger active == broker active == v1 (no drift); 378 decisions / 756 outcomes /
  0 gaps; self-check passed; preconditions not met: 4 joined non-self decisions (needs 2000),
  exploration enabled for 0 of decisions (needs 0.8).

**Exploration is inert in compare mode.** `PolicyRuntime.decide()` (node-py
`policy_runtime.py`) acts on the native decision only when `mode == "auto"`; in `compare` it
acts on the Python reference's greedy choice (`greedy_decision`, `explored: False`,
exploration disabled) and runs native only to diff the greedy result. `status().exploration`
says so (`disabled:mode_compare`). v1 is therefore routing-identical to v0 today; exploration
starts at task 6.3's cutover to `LIVESTACK_POLICY_NATIVE=auto` (gated on ≥ 7 days of compare
with 0 mismatches — earliest ~2026-10-01). Mode was not changed here.

**7.2 not ticked:** its Verify (24 h of explored decisions, explored fraction within ±50% of
expectation, `receipts/exploration-first-day.md`) cannot be met until 24 h after the 6.3
cutover — earliest ~2026-10-02.
