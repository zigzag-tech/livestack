[Service]
# Where this machine is. The fleet ranks by measured distance, which answers
# "how far from here" and never "where is it"; a caller enforcing a region
# policy needs the second. See livestack announce.node_region.
Environment=LIVESTACK_NODE_REGION=na
