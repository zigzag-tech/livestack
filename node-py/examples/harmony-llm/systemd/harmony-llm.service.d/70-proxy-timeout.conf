[Service]
# Bulk historical extraction includes queue time and long 27B generations.
# Match the client ceiling so the facade does not abandon an admitted request first.
Environment=HARMONY_LLM_PROXY_TIMEOUT=900
